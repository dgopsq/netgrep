{
  "name": "@netgrep/example",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "manifest": "node scripts/build-manifest.mjs"
  },
  "//": "This app is the public demo at https://netgrep.diegopasquali.com, so unlike the rest of the repository its dependencies ARE on the maintenance path — see docs/decisions/0017-example-as-hosted-demo.md. Typechecking lives in the root `pnpm typecheck` rather than in `build`, so the TypeScript version stays pinned in one place.",
  "dependencies": {
    "@netgrep/netgrep": "workspace:*",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.27.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "tailwind-merge": "^3.6.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "tailwindcss": "^4.3.3",
    "vite": "^8.1.5"
  }
}
